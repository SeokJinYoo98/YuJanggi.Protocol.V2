# README 작성 지침

이 지침은 이 저장소의 README를 작성하거나 수정할 때 적용한다. 프로토콜과 서버 V2의
문서에 일관된 기준을 적용할 수 있도록 서버용 기준도 함께 기록한다. 이 파일만으로
다른 저장소의 지침이 자동 적용되는 것은 아니다.

## 빌드·테스트 정책 (저장소 전체 공통)

- 이 절은 README 작업뿐 아니라 이 저장소의 모든 코드·문서 작업에 적용한다.
- 빌드와 테스트는 사용자가 직접 수행한다. 사용자가 명시적으로 요청한 경우에만 에이전트가 실행한다.
- 기능 추가·수정·리팩토링 시 자동화 테스트 코드나 테스트 프로젝트를 기본 작업으로 추가하거나 확장하지 않는다. 테스트 작업은 별도 요청이 있을 때 수행한다.
- 검증을 위해 애플리케이션 실행이나 디버깅을 임의로 수행하지 않는다.
- 기본 확인 범위는 코드 읽기, 변경 diff 및 호출 관계 검토로 제한한다.
- 완료 보고와 커밋 본문에는 빌드·테스트를 실행하지 않았으며 사용자 직접 검증 대상임을 명시한다. 미실행을 통과로 표현하지 않는다.
- 사용자가 원하면 확인할 테스트 시나리오나 실행 명령을 안내하되 직접 실행하지 않는다.

## 목적과 기본 원칙

- 처음 방문한 개발자가 프로젝트의 역할, 사용 조건, 시작 방법을 빠르게 파악하도록 작성한다.
- 기본 언어는 한국어로 하고, API 이름·namespace·명령어·설정 키는 코드 표기를 유지한다.
- 첫 문단에는 무엇을 제공하는지와 누가 사용하는지를 1~3문장으로 설명한다.
- 사용자가 실행해 볼 수 있는 최소 예제를 내부 구조 설명보다 앞에 둔다.
- 현재 구현을 설명한다. 계획, 미구현 기능, 미검증 환경은 지원 기능과 구분한다.
- 다른 프로젝트의 문구나 분량을 복제하지 않고, 필요한 구성 방식만 참고한다.

## 작성 전 확인

1. 기존 README, 프로젝트 파일, 진입점, public API, 설정 파일, 테스트, 배포 스크립트를 읽는다.
2. 프레임워크와 언어 버전, 패키지 이름과 버전, 의존성, 실행 경로를 실제 파일에서 확인한다.
3. 배포 가능한 상태와 이미 배포된 상태를 구분한다. 로컬 NuGet 생성 성공만으로
   nuget.org 설치 명령이 동작한다고 쓰지 않는다.
4. 기존 설명 중 유효한 내용을 유지하고, 코드와 어긋난 설명은 수정한다.
5. 서버 README를 작성할 때는 서버 저장소를 직접 조사한다. 프로토콜 코드만 보고
   서버의 포트, 설정, 인증, DB, 실행 명령, 구현 기능을 추측하지 않는다.

## 권장 구성과 순서

아래 순서를 기본으로 하되 해당하지 않는 항목은 생략한다. 빈 섹션을 만들지 않는다.

| 순서 | 구성 | 작성할 내용 |
| --- | --- | --- |
| 1 | 프로젝트 이름과 소개 | 역할, 대상 사용자, 해결하는 문제 |
| 2 | 핵심 기능과 현재 상태 | 구현된 주요 기능 3~6개와 사용에 중요한 제약 |
| 3 | 요구 환경 | SDK, 런타임, Unity 버전 및 사전 의존성 |
| 4 | 빠른 시작 | 설치 또는 준비 → 최소 사용/실행 → 성공 확인 |
| 5 | 핵심 사용법 | 대표 API 또는 클라이언트 요청 예제와 결과 |
| 6 | 구성과 동작 | 필요한 설정, 주요 데이터 흐름, 관련 저장소의 역할 |
| 7 | 개발 및 검증 | 빌드, 테스트, 필요한 경우 패키징 명령 |
| 8 | 제약과 문제 해결 | 실제 제한, 알려진 오류의 원인과 해결 방법 |
| 9 | 관련 문서·기여·라이선스 | 존재하고 확인된 문서와 정책의 링크 |

설치에 직접 영향을 주는 제약은 뒤쪽에만 두지 말고 해당 설치 단계에도 짧게 표시한다.
README가 길어질 때만 목차를 추가한다. GitHub의 자동 목차도 활용한다.

## 프로토콜 라이브러리 README

- Unity 클라이언트와 .NET 서버 사이에서 공유하는 계약의 역할을 설명한다.
- NuGet 사용자와 Unity UPM 사용자의 설치 경로를 분리하고 각 경로의 사전 조건을 적는다.
- 단일 원본의 위치, UPM 생성 명령, 생성 파일을 직접 수정하지 않는 이유를 설명한다.
- 현재 방식이 로컬 UPM 설치라면 이를 명시한다. 생성물이 Git에 없는데 Git URL로
  바로 설치할 수 있다고 안내하지 않는다. 구조 변경 시 실제 지원 방식에 맞게 갱신한다.
- System.Text.Json 및 전이 의존성의 제공 주체를 설명한다. .NET 빌드 통과를
  Unity Editor 또는 IL2CPP 검증 완료와 동일하게 취급하지 않는다.
- 실제 MessageFactory 호출 → MessageSerializer 직렬화 → MessageFramer 프레이밍의
  최소 예제를 제공한다. 수신 예제가 있다면 헤더와 본문을 구분한다.
- public DTO, namespace, ProtocolVersion, 메시지 종류와 식별자의 의미는 소스에서 확인한다.
  패키지 버전과 통신 프로토콜 버전은 별도 개념으로 설명한다.
- 프레임 헤더 크기, 바이트 순서, 본문 길이 제한, 잘못된 입력 처리처럼 연동에 필요한
  규칙을 확인하여 적는다. 소켓 수신과 게임 규칙 등 소비자 측 책임도 명확히 한다.
- 전체 API 나열은 피하고 상세 계약은 별도 문서 또는 소스 링크로 안내한다.

## 서버 V2 README

- 실제 제공하는 서버 기능과 프로토콜·게임 로직·클라이언트의 관계를 설명한다.
- 빠른 시작에는 필요한 저장소/패키지 준비, 설정, 실행 명령, 정상 기동 확인 방법을 넣는다.
- 명령은 실행할 디렉터리와 프로젝트 경로를 명시한다. 종료 방법도 필요한 경우 안내한다.
- 설정은 이름, 용도, 기본값, 필수 여부를 표로 정리한다. 환경변수와 설정 파일의
  우선순위는 구현을 확인한 경우에만 적고, 비밀값 대신 명확한 예시 값을 사용한다.
- 실제 사용하는 전송 방식, 접속 주소·포트, 핸드셰이크와 메시지 처리 흐름을 설명한다.
  TCP 서버에 존재하지 않는 HTTP 엔드포인트나 health check를 만들어 넣지 않는다.
- 개발용 실행과 운영 배포를 구분한다. Docker, DB, 인증, TLS 등은 존재하는 구성만 설명한다.
- 지원 프로토콜 버전과 불일치 처리, 연결 종료, 오류 응답 등 연동자가 알아야 할 동작을 적는다.
- 구조도는 역할과 호출 관계를 이해하는 데 도움이 될 때만 작은 Mermaid 도식으로 제공한다.

## 예제와 표현 규칙

- 코드 블록에는 `csharp`, `powershell`, `json`, `text` 등 언어를 지정한다.
- 복사해서 실행할 명령에는 프롬프트 기호나 실행 결과를 섞지 않는다.
- 예제에는 필요한 using, 입력값, 호출 순서와 예상 결과를 포함한다.
  생략된 코드가 있으면 그대로 실행할 수 없는 예제임을 명시한다.
- 존재하지 않는 API, 파일, URL, 배지, 테스트 결과를 만들지 않는다.
- 배지는 실제 CI·배포·라이선스를 확인할 수 있을 때만 최소한으로 사용한다.
- 성능 우위나 운영 안정성은 근거 없이 주장하지 않는다. 벤치마크를 넣는다면 조건과 출처를 적는다.
- 장식용 로고, 이모지, 스크린샷보다 사용에 필요한 정보를 우선한다.
- 저장소 내부 문서는 상대 링크를 사용한다. 개발자 개인의 절대 경로를 README에 넣지 않는다.
- 폴더 트리는 주요 디렉터리와 역할만 표시하고 bin, obj 등 빌드 산출물은 구조 설명에서 제외한다.
- 긴 API 설명과 운영 절차는 별도 문서로 분리하고 README에서 연결한다.
- 라이선스는 실제 LICENSE를 확인한다. 없으면 특정 라이선스를 임의로 선언하지 않는다.

## 완료 전 검토

- 처음 보는 개발자가 요구 환경부터 첫 성공 결과까지 순서대로 따라갈 수 있는가?
- 명령, 경로, API 이름, 버전, 설정값을 현재 코드와 대조했는가?
- 링크 대상과 문서 내 앵커가 존재하고, 코드 블록과 표가 올바르게 닫혀 있는가?
- 패키지 생성·공개 배포·런타임 검증 상태가 정확히 구분되어 있는가?
- 새로 추가하거나 변경한 실행 예제를 코드와 대조했는가? 실제 실행 검증은 사용자에게 맡긴다.
- 검증하지 못한 부분은 완료 보고에서 명시했는가? 과거 테스트 결과를 현재 실행 결과로 쓰지 않는다.
- 문서·실행 명령·코드 예제를 변경해도 별도 요청 없이 빌드·테스트를 실행하지 않는다.

## 참고한 공개 README

조사일: 2026-09-20. 아래는 널리 사용되는 프로젝트의 구성 사례이며 인기 순위가 아니다.
별 수는 변하므로 지침의 기준으로 고정하지 않는다. 권장 순서는 이 저장소에 맞춰 종합한 것이다.

| 출처 | 관찰한 구성 | 적용할 방식 |
| --- | --- | --- |
| [FastAPI](https://github.com/fastapi/fastapi#readme) | 소개, 기능, 요구 환경, 설치, 코드 작성·실행·결과 확인 | 빠른 시작에 정상 동작 확인까지 포함 |
| [Dapper](https://github.com/DapperLib/Dapper#readme) | NuGet 안내, 기능별 C# 예제, 성능과 제약 설명 | 라이브러리의 대표 사용법을 코드 중심으로 설명 |
| [UniTask](https://github.com/Cysharp/UniTask#readme) | 기능 요약, 목차, 시작 예제, Unity 관련 제약, UPM과 .NET 안내 | 소비 환경별 설치와 제약을 구분 |
| [GitHub README 안내](https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-readmes) | 목적, 유용성, 시작 방법, 도움과 기여 안내 | 첫 방문자가 필요한 정보를 찾는지 점검 |
